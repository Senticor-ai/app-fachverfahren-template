node_modules
dist
**/dist
dist-server
**/dist-server
**/dist-types
coverage
playwright-report
pnpm-lock.yaml
**/public/mockServiceWorker.js
# GENERIERT via `pnpm --filter @senticor/fachverfahren emit:contract` — Format ist der emit-Output, nie Prettier
**/leistung.contract.json
# GENERIERT via `pnpm --filter @senticor/fachverfahren emit:procedure-contract` — Format ist der emit-Output, nie Prettier
**/procedure.contract.json
# GENERIERT via `pnpm --filter @senticor/fachverfahren emit:docs` — komplette Repo-Doku, Format ist der emit-Output
**/docs-manifest.generated.ts
# GENERIERT via `node --experimental-strip-types scripts/emit-leistung-schema.mts` aus der EINEN Pflicht-Form
# (packages/fachverfahren-kit/src/leistung-contract-form.ts) — Format ist der emit-Output, nie Prettier.
# Wuerde Prettier hier umbrechen, schluege das Frische-Gate in check-leistung-contract.mts dauerhaft fehl.
schemas/leistung-config.schema.json
**/deploy/helm/**/templates/**
