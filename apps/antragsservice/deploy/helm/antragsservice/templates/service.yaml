apiVersion: v1
kind: Service
metadata:
  name: {{ include "antragsservice.fullname" . }}
  labels:
    {{- include "antragsservice.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "antragsservice.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "antragsservice.fullname" . }}-internal
  labels:
    {{- include "antragsservice.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "antragsservice.selectorLabels" . | nindent 4 }}
  ports:
    - name: internal
      port: {{ .Values.internalService.port }}
      targetPort: internal
      protocol: TCP
