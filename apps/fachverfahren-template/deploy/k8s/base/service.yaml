apiVersion: v1
kind: Service
metadata:
  name: fachverfahren-web
  labels:
    app.kubernetes.io/name: fachverfahren-template
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fachverfahren-template
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
