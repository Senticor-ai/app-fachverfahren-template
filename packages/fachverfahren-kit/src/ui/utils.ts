// fachverfahren-kit/ui/utils — `cn` (clsx + tailwind-merge). Re-Export der EINEN Kit-Implementierung
// aus ../lib/utils, damit es genau einen cn-Helfer gibt (keine doppelte Wahrheit).
export { cn } from "../lib/utils.js";
